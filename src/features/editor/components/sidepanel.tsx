const SidePanel = () => {
    return (
        <aside className="bg-gray-200 p-4 rounded-lg h-full">
            <h2 className="text-lg font-semibold">Side Panel</h2>
            <p className="text-sm text-gray-600">Subtitle controls & settings.</p>
        </aside>
    );
};

export default SidePanel;
